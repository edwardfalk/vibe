import { dist } from '@vibe/core';
import { TWO_PI } from '@vibe/core';
import { PI } from '@vibe/core';
// TEMP violation for self-learning E2E
export const a = PI + (TWO_PI);
const d = dist(0, 0, 10, 10);
console.log(a, d);
