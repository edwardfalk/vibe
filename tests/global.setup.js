// global.setup.js was used to start a legacy Ticket API before tests.
// That API has been removed. Keep this stub to avoid Playwright errors if referenced.
export default async function noopGlobalSetup() {
  // no-op
}
